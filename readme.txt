On n'est pas ici pour philosopher, Parmentier.

# How to view the demo

1. Clone or download this repository.
2. Open `index.html` in your preferred web browser. You can double-click the file or serve it with a simple local server such as `python -m http.server`.

The sample text contains phrases in the `/word:explanation/` format. Click the highlighted words to reveal their explanations.
